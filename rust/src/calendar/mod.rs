pub fn write_file_data_to_calendar(name: &str, rgb_color: (u8, u8, u8)) {
    // TODO:
}

pub fn write_color_to_calendar_file(name: &str, rgb_color: (u8, u8, u8)) {
    // TODO:
}

pub fn write_calendar_data_to_file(name: &str) {
    // TODO:
}
